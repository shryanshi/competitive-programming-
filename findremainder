#include <iostream>
using namespace std;
int main()
{
    int a, r;
    cin >> a;
    while (a--)
    {
        // int r;
        long long int a, b;
        cin >> a >> b;
        cout<<a % b<<endl;
        // cout << r;
    }
    

    return 0;
}

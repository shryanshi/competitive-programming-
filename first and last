#include <bits/stdc++.h>

using namespace std;

int main()
{

    int t;
    cin >> t;
    string s;
    while (t--)
    {
        cin >> s;
        int l = s.size();
        cout << (s[0] - '0') + (s[l - 1] - '0') << endl;
    }
    return 0;
}

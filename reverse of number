#include <iostream>
using namespace std;
int main()
{
    int n, a, count = 0;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> a;
        while (n--)
        {
            a = a/10;
            // count++;
            string y = y + (string)a;
        }
        // for (int j = 0; j < count; j--)
        // {
        //     /* code */
        // }
    }
    return 0;
}
